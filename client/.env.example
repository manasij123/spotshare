# Base URL of the SpotShare backend API. In development the Vite dev server
# proxies /api to this target (see vite.config.ts), so this is mainly used
# for production builds served separately from the API.
VITE_API_BASE_URL=""
